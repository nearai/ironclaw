---
title: Docker Installation
sidebarTitle: Docker
description: Run T3Claw in a Docker container
---

Run T3Claw inside a Docker container. This provides isolation and consistency across environments.

<Warning>
**Important distinction:** T3Claw runs **alongside** Docker (for job sandboxing), not inside Docker by default. This page covers running T3Claw itself in a container — a different use case from the default installation.
</Warning>

## When to Use Docker

- **Testing**: Quick experiments without modifying your system
- **Consistent environments**: Same configuration across dev/staging/prod
- **Multi-tenant**: Run multiple T3Claw instances on one host
- **CI/CD**: Automated deployments

## Quick Start

```bash
# Create data directory
mkdir -p ~/.t3claw

# Run T3Claw
docker run -d \
  --name t3claw \
  -v ~/.t3claw:/home/t3claw/.t3claw \
  # enabling this mount will allow the container to control the docker daemon, use at own risk
  # -v /var/run/docker.sock:/var/run/docker.sock \
  -p 3000:3000 \
  -p 8080:8080 \
  # Pin to a specific T3Claw version for reproducible, rollback-friendly deployments.
  Terminal-3/t3-claw:latest 
```

## Docker Compose

Save as `docker-compose.yml`:

```yaml
version: '3.8'

services:
  t3claw:
    # Pin to a specific T3Claw version for reproducible, rollback-friendly deployments.
    image: Terminal-3/t3-claw:latest
    container_name: t3claw
    restart: unless-stopped

    volumes:
      # Persistent data
      - ~/.t3claw:/home/t3claw/.t3claw

      # Docker socket for sandbox jobs, also exposes your docker host to this container!
      - /var/run/docker.sock:/var/run/docker.sock

    ports:
      # Web Gateway
      - "3000:3000"
      # HTTP Webhook
      - "8080:8080"

    environment:
      # Required: database backend
      - DATABASE_BACKEND=libsql

      # Optional: LLM backend
      - LLM_BACKEND=nearai
      - NEARAI_SESSION_TOKEN=${NEARAI_SESSION_TOKEN}

      # Optional: Web Gateway
      - GATEWAY_ENABLED=true
      - GATEWAY_HOST=0.0.0.0
      - GATEWAY_PORT=3000

      # Optional: HTTP Webhook
      - HTTP_ENABLED=true
      - HTTP_HOST=0.0.0.0
      - HTTP_PORT=8080

# Optional: PostgreSQL instead of libSQL
#  postgres:
#    image: pgvector/pgvector:pg15
#    environment:
#      POSTGRES_USER: t3claw
#      POSTGRES_PASSWORD: changeme
#      POSTGRES_DB: t3claw
#    volumes:
#      - postgres_data:/var/lib/postgresql/data
#
#volumes:
#  postgres_data:
```

Start:

```bash
docker compose up -d
```

## Volume Mounts

| Host Path | Container Path | Purpose |
|-----------|---------------|---------|
| `~/.t3claw` | `/home/t3claw/.t3claw` | Config, database, logs |
| `/var/run/docker.sock` | `/var/run/docker.sock` | Launch sandbox containers |

## Environment Variables

Pass configuration via environment variables:

```bash
docker run -d \
  --name t3claw \
  -e DATABASE_BACKEND=libsql \
  -e LLM_BACKEND=nearai \
  -e NEARAI_SESSION_TOKEN=sess_xxx \
  -e GATEWAY_ENABLED=true \
  -v ~/.t3claw:/home/t3claw/.t3claw \
  -v /var/run/docker.sock:/var/run/docker.sock \
  -p 3000:3000 \
  Terminal-3/t3-claw:latest
```

See [Configuration Reference](/setup/configuration) for all options.

## Docker-in-Docker Considerations

T3Claw can launch sandbox containers. In Docker, this requires:

1. **Docker socket mount** (shown above): Allows T3Claw to launch sibling containers
2. **Privileged mode** (optional): Only if sandbox jobs need elevated permissions

```bash
# With privileged mode (not recommended unless needed)
docker run -d \
  --name t3claw \
  --privileged \
  -v ~/.t3claw:/home/t3claw/.t3claw \
  -v /var/run/docker.sock:/var/run/docker.sock \
  ...
```

## Custom Data Directory

To use a different data location:

```bash
mkdir -p /opt/t3claw/data

docker run -d \
  --name t3claw \
  -e T3CLAW_BASE_DIR=/data \
  -v /opt/t3claw/data:/data \
  ...
```

## Reverse Proxy (HTTPS)

For external access, put a reverse proxy in front:

### Caddy

```caddy
# Caddyfile
webg.example.com {
  reverse_proxy localhost:3000
}
```

### nginx

```nginx
server {
  listen 443 ssl;
  server_name webg.example.com;

  ssl_certificate /path/to/cert.pem;
  ssl_certificate_key /path/to/key.pem;

  location / {
    proxy_pass http://localhost:3000;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
  }
}
```

## Updating

```bash
# Pull latest image
docker pull Terminal-3/t3-claw:latest

# Recreate container
docker stop t3claw
docker rm t3claw
docker run -d \
  --name t3claw \
  -v ~/.t3claw:/home/t3claw/.t3claw \
  -v /var/run/docker.sock:/var/run/docker.sock \
  -p 3000:3000 \
  Terminal-3/t3-claw:latest

# Or with docker compose
docker compose pull
docker compose up -d
```

## Troubleshooting

<AccordionGroup>
  <Accordion title="Cannot connect to Docker daemon" icon="x-circle">
    Ensure Docker socket is mounted:
    ```bash
    docker run ... -v /var/run/docker.sock:/var/run/docker.sock ...
    ```
  </Accordion>

  <Accordion title="Permission denied on data directory" icon="lock">
    Fix ownership:
    ```bash
    sudo chown -R 1000:1000 ~/.t3claw
    ```
  </Accordion>

  <Accordion title="Port already in use" icon="network">
    Change port mapping:
    ```bash
    -p 3002:3000  # Maps host port 3002 to container port 3000
    ```
  </Accordion>
</AccordionGroup>

## Next Steps

<CardGroup cols={2}>
  <Card title="Configuration" icon="settings" href="/setup/configuration">
    Full environment variable reference
  </Card>
  <Card title="VPS Install" icon="server" href="/install/vps">
    Production deployment guide
  </Card>
</CardGroup>
