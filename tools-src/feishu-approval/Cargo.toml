[package]
name = "feishu-approval-tool"
version = "0.1.0"
edition = "2021"
description = "Feishu/Lark approval tool for IronClaw (WASM component)"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
wit-bindgen = { workspace = true }

[lib]
crate-type = ["cdylib"]
