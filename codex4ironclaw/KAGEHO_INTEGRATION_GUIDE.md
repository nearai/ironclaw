| **INTEGRATION.md** | • Worker reachable at `ws://192.168.1.157:9090/ws/age
nt` using `AGENT_AUTH_TOKEN`. <br>• Two prompt styles: code generation (retu
rns text/code) and filesystem edit (writes files in `/workspace`). <br>• Mou
nt your repo into the worker (`/home/sun/myproject:/workspace:rw`). <br>• Po
st‑processor routine recommended to parse `<DIFF>`, `<CODE>`, `<FILES>` bloc
ks and apply them safely. |
| **HEALTH_CHECK.md** | • Health endpoint: `http://192.168.1.157:9090/health
` (expects `200 OK`). <br>• Routine (cron `0 */5 * * * *`) checks every 5 mi
n, tracks consecutive failures, and restarts the worker after 2 failures (vi
a admin endpoint, Docker‑compose command, or manual alert). |
| **IRONCLAW_0.25.0_RELEVANT.md** | • New tools subsystem, versioned WASM ex
tensions – can package a custom XMPP bridge as a channel/tool. <br>• Deploy 
profiles (`IRONCLAW_PROFILE`) let you keep a separate `custom‑bridge` profil
e. <br>• Granular tool permission system – restrict bridge usage to privileg
ed agents. <br>• Workspace versioning & patch handling – safe for Codex‑generated patches. <br>• Rate‑limit endpoint now returns `retry‑after=None`, mat
ching our `xmpp‑rate‑limit.sh`. |
| **HEALTH_CHECK.md** (again) | • Details on restart logic, state tracking, 
and escalation. <br>• Options: local admin restart endpoint (preferred), Doc
ker‑compose restart via API, or notify‑only. |

**How this helps you**

- **Deploy** a custom XMPP bridge with its own profile and limited tool acce
ss.  
- **Automate** health monitoring & auto‑restart of the Codex worker.  
- **Safely apply** code generated by Codex using the recommended `<DIFF>`/`<
CODE>` contracts.   
- **Leverage** the new tools subsystem to ship the bridge as a WASM channel 
if desired.
---

**Next steps you might want**

- Create the health‑check routine in IronClaw.  
- Draft a post‑processor routine to handle Codex output blocks.  
- Generate a starter monorepo layout (apps / packages) for the bridge code.
