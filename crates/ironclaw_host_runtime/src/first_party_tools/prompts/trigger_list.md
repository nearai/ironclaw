List scheduled triggers owned by the current caller scope.

Use returned ids and raw schedule fields only as internal inputs for follow-up trigger capabilities.

In the user-facing reply, call them routines and summarize names, status, and schedules in plain language; never expose trigger, agent, project, or delivery-target ids, raw cron, stored prompts, internal capability names, result references, or host metadata.
