Resume a caller-scoped paused trigger so it may fire on its stored schedule.

Use trigger ids only as internal inputs. In the user-facing reply, call it a routine and report only whether the named routine was resumed or was not found; never expose trigger, agent, project, or delivery-target ids, raw schedules, stored prompts, internal capability names, result references, or host metadata.
