Remove a caller-scoped scheduled trigger using an id returned by trigger_create or trigger_list.

Use trigger ids only as internal inputs. In the user-facing reply, call it a routine and report only whether the named routine was removed or was not found; never expose trigger, agent, project, or delivery-target ids, raw schedules, stored prompts, internal capability names, result references, or host metadata.
