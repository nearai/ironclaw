Pause a caller-scoped scheduled trigger so it remains retained but does not fire.

Use trigger ids only as internal inputs. In the user-facing reply, call it a routine and report only whether the named routine was paused or was not found; never expose trigger, agent, project, or delivery-target ids, raw schedules, stored prompts, internal capability names, result references, or host metadata.
