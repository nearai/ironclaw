{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "job_id": {
      "type": "string",
      "description": "The job_id whose result to read (one listed in the brief)."
    },
    "offset": {
      "type": "integer",
      "description": "Byte offset to start from (default 0). Pass next_offset to page a long result.",
      "minimum": 0
    }
  },
  "required": [
    "job_id"
  ],
  "additionalProperties": false
}
