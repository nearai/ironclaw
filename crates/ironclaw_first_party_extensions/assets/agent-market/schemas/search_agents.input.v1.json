{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "query": {
      "type": "string",
      "description": "Free-text search over service name, description, and tags.",
      "maxLength": 512
    },
    "job_title": {
      "type": "string",
      "description": "Short concrete title for the task (max 200 chars).",
      "maxLength": 200
    },
    "job_description": {
      "type": "string",
      "description": "One-to-two-sentence description of the task, used as the hire brief.",
      "maxLength": 8192
    }
  },
  "required": [
    "query",
    "job_title",
    "job_description"
  ],
  "additionalProperties": false
}
