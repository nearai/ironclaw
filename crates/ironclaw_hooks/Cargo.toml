[package]
name = "ironclaw-hooks"
version = "0.1.0"
edition = "2021"
rust-version = "1.92"
description = "IronClaw lifecycle hook traits — shared interface for proof_of_claw and ironclaw"
authors = ["Proof of Claw <proof@proofofclaw.xyz>"]
license = "MIT OR Apache-2.0"

[dependencies]
async-trait = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
tracing = "0.1"
