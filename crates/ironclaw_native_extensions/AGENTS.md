# Agent Notes

- Read `CLAUDE.md` before adding providers.
- Keep provider modules isolated from sibling provider modules.
- This crate registers native provider assets; composition owns merging outputs into runtime registries.
