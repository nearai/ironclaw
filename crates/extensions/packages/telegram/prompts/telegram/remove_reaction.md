Telegram clears all of the linked account's reactions on a message; it cannot remove one named emoji while leaving others. The result therefore omits `emoji`, even when one was supplied, rather than claiming a specific reaction was removed.

The host selects this operation from the capability id. Provide only the parameters described by the input schema; do not include an action field.
