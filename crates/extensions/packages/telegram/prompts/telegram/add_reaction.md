`emoji` is the unicode emoji itself, for example `👍` — not a `:shortname:`. Each chat permits only a specific set of reactions, and an emoji outside that set is rejected.

The host selects this operation from the capability id. Provide only the parameters described by the input schema; do not include an action field.
