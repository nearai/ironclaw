Create a new Google Docs document.

Create only the requested deliverable. Do not create scratch documents to discover table indexes; use `inspect_document` and the semantic editing operations instead.

The host selects this operation from the capability id. Provide only the parameters described by the input schema; do not include an action field.
