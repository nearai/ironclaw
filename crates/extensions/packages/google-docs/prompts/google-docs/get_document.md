Get document metadata: title, revision, body length, and named ranges.

This operation does not return paragraphs or tables. Use `inspect_document` for document structure and indexed content.

The host selects this operation from the capability id. Provide only the parameters described by the input schema; do not include an action field.
