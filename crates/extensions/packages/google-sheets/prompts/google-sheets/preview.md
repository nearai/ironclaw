# google-sheets.preview

Use this to understand a spreadsheet shape before reading or editing larger ranges.

Prefer this over `google-sheets.read_values` when the user asks for a summary, digest, "what is in this sheet", or when you need to inspect headers before selecting a precise range.

The result includes sheet dimensions, headers, sampled rows, and the exact range read.
