# ironclaw_event_projections

Canonical crate guidance lives in [`AGENTS.md`](./AGENTS.md) (working rules,
the `error_detail` sanitization ladder, dependency boundary) and
[`README.md`](./README.md) (orientation, public surface, gates). This file is
a pointer so the two never drift — one canonical home per rule
(`docs/reborn/guidance-conventions.md`).
