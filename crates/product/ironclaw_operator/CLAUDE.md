# ironclaw_operator

Consolidated per `docs/reborn/guidance-conventions.md` (one canonical home per
fact): the working rules — the one rule (implements ports, never declares
them, never depends on `ironclaw_assistant`), the port inventory, route
carrier discipline, vendor-name licensing, known debt, and testing split —
live in [`AGENTS.md`](./AGENTS.md); orientation in [`README.md`](./README.md);
the family boundary in [`crates/product/AGENTS.md`](../AGENTS.md). This crate
has no module spec — it is not in the root `CLAUDE.md` Module Specs table.
