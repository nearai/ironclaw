# ironclaw_host_ingress

Consolidated per `docs/reborn/guidance-conventions.md` (one canonical home per
fact): the working rules — the all-kinds dependency allowlist and the
`substrates` layer note — live in [`AGENTS.md`](./AGENTS.md); orientation in
[`README.md`](./README.md); the family boundary in
[`crates/product/AGENTS.md`](../AGENTS.md). This crate has no module spec — it
is not in the root `CLAUDE.md` Module Specs table.
