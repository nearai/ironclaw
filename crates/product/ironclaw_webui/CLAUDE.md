# ironclaw_webui

The crate's **module spec** is [`CONTRACT.md`](./CONTRACT.md) — named in the
root `AGENTS.md` Module Specs table; code follows spec, spec is the
tiebreaker. Working rules: [`AGENTS.md`](./AGENTS.md); orientation:
[`README.md`](./README.md). This file is a pointer, kept so tooling that
loads `CLAUDE.md` lands there — do not add rules here (one canonical home per
fact, `docs/reborn/guidance-conventions.md`).
