# ironclaw_assistant

Canonical guidance for this crate lives in [`AGENTS.md`](./AGENTS.md) — the
working rules and the **enforced `reborn_services` module-charter map**
(`tests/reborn_services_module_charter.rs` reads that file's map section, so
it stays the rules' one home). Orientation is [`README.md`](./README.md).
This file is a pointer, kept so tooling that loads `CLAUDE.md` lands there —
do not add rules here (one canonical home per fact,
`docs/reborn/guidance-conventions.md`).
