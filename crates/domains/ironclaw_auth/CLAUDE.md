# ironclaw_auth

Canonical guidance for this crate lives in [`AGENTS.md`](./AGENTS.md) — the
guardrails and the **enforced sub-owner map** (`cargo test -p ironclaw_auth
--test module_charter` reads that file's `## Sub-owner map` section, so it
stays the rules' one home). Orientation is [`README.md`](./README.md). This
file is a pointer, kept so tooling that loads `CLAUDE.md` lands there — do not
add rules here (one canonical home per fact,
`docs/reborn/guidance-conventions.md`).
