Record this run's final structured result. This tool's parameters ARE the
output schema the caller declared for this run: call it exactly once, with
arguments that fully satisfy that schema, when your work is complete. The
validated arguments become the run's terminal output — there is no
assistant-message final answer on this run, and plain-text replies are
rejected. If a call is rejected, fix the listed schema violations and call
again with corrected arguments.
