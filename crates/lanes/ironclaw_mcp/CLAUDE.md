# ironclaw_mcp

Canonical guidance for this crate lives in [`AGENTS.md`](./AGENTS.md)
(working rules, gate-pinned by `tests/module_charter.rs`) and
[`README.md`](./README.md) (orientation); the seven-module charter table is
the `src/lib.rs` doc comment. This file is a pointer, kept so tooling that
loads `CLAUDE.md` lands there — do not add rules here (one canonical home per
fact, `docs/reborn/guidance-conventions.md`).
