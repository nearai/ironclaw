//! Feature-gated test-support fixtures for adapter consumers (never compiled
//! into production binaries; see `scripts/check_no_panics.py` for the
//! canonical module convention).

pub mod conformance;
